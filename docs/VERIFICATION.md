# Журнал проверок

Среда: песочница, 4 общих CPU, 15 ГБ RAM, PostgreSQL 16 + PostGIS 3.4, Python 3.11, Node 22, Chromium
(SwiftShader WebGL) для Playwright. **Внешняя сеть закрыта** (открыты только npm, PyPI, GitHub, apt),
поэтому всё ниже проверено на синтетических источниках devstand и офлайн-подмножестве GeoNames.
«Проверено» здесь значит: запущено, результат наблюдался, цифры получены прогоном, а не оценкой.

## Сводка

| Что | Как | Результат |
|---|---|---|
| Unit и интеграционные тесты | `pytest -q` | **41/41** |
| Геолокация, golden-набор (45 случаев, 8 языков: ru uk en de fr es it zh; red-team случаи) | `eval_geoparse` | **45/45**, 6,3 мс на документ |
| Сквозной конвейер против gold-разметки devstand (59 сообщений, 21 источник) | `eval_pipeline` | место 58/59 (0,983); кластеры B-cubed **P = 1,000, R = 0,974, F1 = 0,987**; ошибочных склеек **0** |
| Критерий готовности (14 шагов ТЗ) в браузере, dev-сервер | `e2e/acceptance.mjs` | **14/14**, ошибок страницы 0 |
| То же в Docker: `docker compose`, прод-сборка за nginx, чистая БД | `e2e/acceptance.mjs` | **14/14**, ошибок страницы 0 |
| То же в продакшн-конфигурации: HTTPS через Caddy, 2 процесса API, бэкапы | `scripts/prod.sh` + `e2e/acceptance.mjs https://localhost` | **14/14**, ошибок страницы 0 |
| Согласованность rollup и событий до и после live-обновлений | `scripts/check_rollup.sql` | 0 расхождений на всех 5 уровнях |
| Нагрузка: 1,05 млн событий, 50 тыс. в одном городе | `scripts/loadtest.py` | см. раздел «Производительность» |

## Критерий готовности (Playwright, `frontend/e2e/acceptance.mjs`)

1. Открыть Землю: глобус, пузыри по континентам.
2. Страна: поиск «Россия» → панель страны.
3. Регион: «Краснодарский край», 23 события, «Где происходит» с дочерними местами.
4. Крупный город: «Краснодар» (омоним Краснодарского края выбирается по контексту в выдаче).
5. Малый город: «Горячий Ключ».
6. Посёлок или деревня: «хутор Каштаны» (600 жителей) — лента из 1 события.
7. Точка: «станица Динская».
8. Связанные новости: 3 события в Динской.
9. Несколько разных источников: пожар в Динской, 7 источников (Telegram ×2, СМИ ×2, МЧС, YouTube, федеральное агентство).
10. Объединённые дубли: пересылка Telegram помечена копией; «7 источников · 6 независимых».
11. Открыть оригинал: ссылка ведёт на страницу поста на стенде.
12. Таймлайн: ≥5 записей — первое сообщение, пересылка, отчёт МЧС, обновления.
13. Без перезагрузки: `POST /control/release` → через 20–45 с тост «ДТП на въезде в Новотитаровскую», бейдж «1 новое».
14. «Почему здесь?»: станица Новотитаровская, фрагмент «Новотитаровскую», 1 из 1 источников.

Тест требует свежего стенда (проверяет предусловие) и падает, если после выбора результата поиска
снова открывается выпадающий список (регрессия, найденная в этой сессии).

## Геолокация: что покрывает golden-набор

Омонимы (Ивановка ×18, Paris FR/US, Moscow RU/ID, Springfield); «в 15 км от Краснодара»; «под Краснодаром»;
улица с названием города («ул. Ростовская»); клубы с названием города («ФК «Краснодар»»); регион без города
(«по всему краю»); несколько мест в одной статье; отсутствие места; координаты в тексте; аппозиции
(«Moscow, Idaho»); падежи ru/uk; de, fr, es, it, zh. После каждого исправления набор прогонялся целиком.

Единственный сквозной промах — `dier-4`: немецкая деревня Dierfeld (7 жителей), упомянутая **по-русски**
(«Дирфельд»). В офлайн-данных у неё нет русского названия, а транслитерационного нечёткого поиска пока нет.
Из-за этого же сообщения событие распалось на два (единственный split в B-cubed).

## Red team: найдено и исправлено

**Геолокация и конвейер**
- «Краснодарского края» не распознавалось (строчное «края») → слова-типы в продолжении n-граммы.
- Город и одноимённая админ-единица конкурировали → общий референт, а не соперники.
- Клубы в кавычках («ФК «Краснодар»», «Ростов») → признак организации и слабая связь `about`.
- «Moscow, Idaho» → аппозиции.
- Расстояние из другого упоминания терялось → привязка признаков к упоминанию.
- Улица без слова «улица» («на Красной»), перечни улиц.
- «На окраине» ошибочно считалось «рядом» → окраина = внутри места.
- «Ивановка» без контекста → высокая неоднозначность, на карту не ставится.
- Межъязыковые пары не сходились → фонетические ключи имён, бонус места при разных языках.
- Официальное «возгорание ликвидировано» и «загорелся дом» не склеивались → совпадение типа события снимает вето по тексту.
- Отключение света путалось с происшествиями → тип `utility_outage` в «Обществе».
- Склеенный заголовок Telegram, форварды с другим хвостом → кандидаты по заголовку.
- Копия, обработанная раньше оригинала → разворот направления.
- Независимые источники считались вместе с пересылками → независимость только по оригинальному контенту.
- Ежедневный прогноз одного источника склеивался со вчерашним (онлайн и в фоновом слиянии) → правило регулярной рубрики (общая функция).
- Сломанный XML давал «0 записей» вместо ошибки → `FetchError`, источник виден как неисправный.

**Интерфейс**
- Воркер MapLibre не грузился в dev → `setWorkerUrl`; **в прод-сборке** он же давал 404 на общий чанк (`?url`) → `?worker&url`.
- Высота контейнера карты 0; цвета и палитра не применялись при загрузке; наложение подписей пузырей.
- Пузырь «Россия» стоял в Сибири → центр как среднее единичных векторов из rollup.
- «станица» показывалась как «Город»; бейдж языка при совпадающем языке; просвечивающая панель; «1 новых».
- После выбора результата поиска список открывался снова и закрывал центр карты.
- Подпись подложки дублировала подпись пузыря («Краснодар» дважды, «РОССИЯ» под «Россия») → базовые подписи мест с пузырём скрываются фильтром.
- На телефоне нельзя было переключить язык (кнопка скрыта), а поиск не сжимался и выталкивал кнопки за край → скрывается декоративный логотип, поиск сжимается; проверено на 360 и 390 px.
- Тост о новом событии не приходил из-за узкого bbox подписки → live-зона с запасом вокруг вьюпорта.

**Карта и геоданные** (найдено на скриншотах этой сессии)
- Тайлы у антимеридиана выбирали противоположное полушарие: рамка с отступом переводилась в 4326 и
  заворачивалась (183,6° → −176,4°). На z1 пропадала почти вся Евразия и Африка, на любом зуме — крайние
  столбцы тайлов (Чукотка, Камчатка, Фиджи). Регрессионный тест падал до исправления.
- Полигоны world-atlas сшиты через антимеридиан; плоское чтение давало полосы через полмира (площадь
  России 20,3 млн км² вместо 17,0 млн, кольца у полюса). Теперь долготы разворачиваются, полярные кольца
  замыкаются через полюс, разрез идёт по ±180 через `ST_WrapX`. После переимпорта: Россия 16,97 млн км²,
  Германия 357 тыс., Фиджи 18,3 тыс.
- Тайлы южного полушария отдавали 500 **только в Docker**: Антарктида теперь доходит до полюса, а PROJ 7.2
  в образе postgis падает на ST_Transform(−90°) (локальный PROJ 9.4 молча возвращал y ≈ −2,4·10⁸ м). Геометрия
  обрезается рамкой тайла в пределах широт Mercator (±85,0511°) до трансформации; заодно большие полигоны
  не трансформируются целиком для каждого тайла.

**Безопасность и легальность**
- XSS через `javascript:`-ссылки в фидах → наружу только http(s) (`safe_url`) и на сервере, и в UI.
- Разметка в заголовках → заголовок проходит через `html_to_text`.
- SSRF через ссылки HTML-листинга → только ссылки того же сайта и только http(s).
- robots.txt при 5xx или недоступности хоста считался разрешающим → по RFC 9309 теперь это полный запрет (тест падал до исправления).
  Перепроверка через 60 с: при 10 минутах сборщик, стартовавший на секунду раньше стенда, из-за backoff простаивал
  до ~20 минут. `devctl` теперь запускает стенд раньше сборщика.
- У кнопки очистки поиска была подпись «Закрыть» (экранные дикторы называли её неверно, e2e-тест нажимал её вместо
  закрытия карточки) → «Очистить поиск».
- Квадратичный самоджойн в фоновом слиянии → инкрементальное слияние по недавно изменённым событиям.

**Развёртывание** (найдено при первом запуске в Docker)
- Healthcheck БД срабатывал на временном init-сервере образа postgis (unix-сокет) → проверка по TCP.
- При обычной установке пакета миграции `.sql` в образ не попадали → `package-data`.
- `apk` недоступен в песочнице → скрипт данных переписан на POSIX sh.
- `devctl.sh start` без аргументов не запускал `maintenance` (фоновое слияние и очистку).
- Nginx разрешал имя `api` один раз при старте: после пересоздания контейнера API веб отдавал 502 → разрешение
  через DNS Docker на каждый запрос. Проверено принудительной сменой IP API (172.18.0.5 → 172.18.0.7) без
  перезапуска web.

## Продакшн-стек (`docker-compose.prod.yml`, `scripts/prod.sh`)

Проверено в песочнице с `DOMAIN=localhost`: Caddy выдаёт локальный сертификат вместо Let's Encrypt.
Источники — демо-стенд.

| Шаг | Результат |
|---|---|
| `init` на пустом сервере (схема, категории, газеттир) | 4 мин 15 с, 271 761 место |
| HTTP → HTTPS | 308 на `https://localhost/` |
| Заголовки | HSTS, `nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy`; HTTP/2 и HTTP/3 (`alt-svc: h3`) |
| Живые обновления через Caddy → nginx → API | SSE приходит сразу (`flush_interval -1`), тост в приёмке через ~10 с после выпуска |
| Наружу открыты | только 80/443 (Caddy); БД, API и веб доступны лишь внутри сети Docker |
| Бэкап при старте и по команде `backup` | дамп 32 МБ, ротация `BACKUP_KEEP` |
| `restore` после намеренной порчи (удалены связи событий) | 39 с; 56 связей восстановлены, сайт поднялся сам |
| `geonames RU` без сети | понятная ошибка, недокачанных файлов нет, том принадлежит пользователю `geonews` |
| `verify-sources` без сети | «nothing verified, no registry written»: пустой реестр не создаётся |

Не проверено здесь:
- **выпуск настоящего сертификата Let's Encrypt** — нужен публичный домен, указывающий на сервер;
- **`scripts/prod.sh init/up` вместе со сборкой образов.** В песочнице pip и npm ходят через прокси с собственным
  сертификатом, которого `compose build` не видит. Образы собраны вручную, в остальном скрипт выполнялся
  без изменений. На обычном VPS прокси нет;
- **скачивание образа Caddy с Docker Hub** — с общего IP песочницы Docker Hub отвечал 429, образ взят из
  официального зеркала `mirror.gcr.io`.

### Установка на сервер владельца и автообновление

На реальном VPS (Ubuntu 22.04) установка одной командой собрала образы, загрузила GeoNames RU
(418 797 населённых пунктов) и проверила источники (9 рабочих). Caddy не запустился: порт 80 занят другим
контейнером. Внешняя проверка (`.github/workflows/site-check.yml`, запуск из GitHub Actions) показала, что на
80 отвечает чужой Caddy с редиректом на HTTPS. Сертификата для имени sslip.io у него нет. Отсюда доработки
установщика; каждая проверена имитацией в песочнице:

| Сценарий | Результат |
|---|---|
| Порт 80 свободен | `.env` не меняется, сайт на 80/443 |
| 80 и 8080 заняты чужими сервисами | `HTTP_PORT=8081`, `SITE_SCHEME=http://` |
| 80 занят нашим же Caddy (повторный запуск) | `.env` не меняется. Раньше проверка по маске `*-caddy-1` путала наш Caddy с чужим из другого compose-проекта |
| Установка прошлой версии уже записала `HTTP_PORT=8080` | дописывается `SITE_SCHEME=http://` |
| Caddy с `SITE_SCHEME=http://` (настоящий `caddy:2-alpine` + nginx) | 200 по `Host: <IP>:8080` и `Host: <sslip>:8080`, попыток выпустить сертификат нет |
| `autoupdate`: новых коммитов нет | выход без действий |
| `autoupdate`: новый коммит меняет кандидатов | обновление, `verify-sources --keep-previous`, `up`, проверка здоровья |
| `autoupdate`: после обновления сайт не отвечает | откат на прежний коммит, `up`. Коммит записан в `.autoupdate.failed`, повторно его не пробуют |

### Сбор без RSS (`tests/unit/test_connectors_web.py`)

- Битый XML в фиде (`&` без экранирования, незакрытый `<br>`) чинится, запись не теряется.
- `html_list` без XPath берёт только статьи своего сайта: теги, разделы, короткие ссылки, якоря и чужие
  домены отсекаются. Уже собранные адреса повторно не скачиваются.
- `sitemap_news`: из индекса карт выбирается новостная (по пути, а не по имени хоста: на `news.test` баг
  выбирал и `sitemap-pages.xml`, исправлено). Старше 72 ч не берётся, порядок от новых к старым, заголовок
  берётся из карты.
- Проверка кандидата без фида выбирает news sitemap раньше разбора главной.
- `respect_robots: false` действует только на свой источник. В реестр попадает отметка «owner's decision».
- `check-sources --keep-previous` сохраняет временно недоступный источник и не добавляет новый неработающий.
- Демо-источник `html_list` на devstand с новым общим кодом статей даёт тот же результат, что и прежний код.

## Отказоустойчивость источников

- «Нестабильный» источник (503): экспоненциальный backoff, после восстановления снова опрашивается в
  непрерывном режиме. Баннер «N из 21 источника временно недоступны».
- «Сломанный XML»: ошибка фиксируется у источника, конвейер не падает.
- Межъязыковая пара, обработанная в «неудобном» порядке («Вибух у Києві» раньше английских сообщений), может
  сначала дать два события; фоновое слияние склеивает их за ~30 с (наблюдалось: событие 10 влито в 12).
- Все источники недоступны (devstand остановлен): все 21 источник помечены неисправными, API отвечает `ok`,
  карта и ленты работают на собранных данных, баннер «21 из 21 источника временно недоступны».

## Производительность

1 050 000 событий (из них 50 000 в Москве), один процесс uvicorn, 4 общих ядра. Холодный запрос / из кэша агрегатов:

| Запрос | Время |
|---|---|
| Континенты | 51 / 5 мс |
| Страны за 7 дней | 246 / 18 мс |
| Регионы Европы за 24 ч | 237 / 63 мс |
| Регионы за 7 дней с фильтром категорий | p50 360, p95 637 мс / 74 мс |
| Населённые пункты по bbox | 96 / 12 мс |
| Плотный город (Москва, 50 тыс. событий) | ≈213 мс |
| Лента Москвы (keyset-пагинация) | 110–146 мс |
| Поиск мест | ≈10 мс |
| Векторный тайл подложки | ≈2 мс |
| Смешанная нагрузка, 8 параллельных клиентов | 21,5 запроса/с |
| Массовое удаление 1 млн событий | 30 с (было >20 мин до индекса на `event.merged_into`) |

До rollup глобальные агрегаты на том же объёме занимали секунды. Самый медленный запрос — окно 7 дней с
фильтрами без кэша (p95 637 мс). Следующий шаг для него — дневной rollup.

## Живые данные (`.github/workflows/live-check.yml`)

Прогон из сети GitHub: проверка кандидатов, полная GeoNames RU, один сбор всех рабочих источников, конвейер,
отчёт `scripts/live_report.py` (что где поставлено и почему). Раннеры GitHub стоят за рубежом: часть
российских сайтов, которые отвечают только российским адресам, здесь недоступна, на сервере в России их больше.

Первый прогон: 15 источников, 1322 материала, 81 % привязаны к месту. Разбор отчёта нашёл ошибки, которых не
было в синтетическом корпусе:

| Что было | Причина | Исправление |
|---|---|---|
| «встречу **Путина**» → деревня Путина (Псковская обл.), событие из 28 статей | фамилия совпала с деревней | фамилия/имя для места < 10 тыс. требует «в/под X», слово-тип или район в тексте |
| «**Зеленский** хотел…» → хутор на Кубани | то же, «свой регион» источника не спасал | собственная территория источника для фамилий не считается |
| «в **Лиге** наций», «на **Кубке**», «Территория **Победы**» → сёла | обычное слово после предлога | для маленького места нужен район в тексте |
| «В **Виннице** прогремел взрыв» → деревня Винницы (Ленобласть) | федеральному СМИ бонус «своя территория» давался на всю страну | бонус +3.5 только региональным и местным источникам |
| «главой **МИД**» → город Mid | аббревиатура | слово целиком заглавными — не населённый пункт |
| «над **Черным** морем» → посёлок Чёрный | прилагательное природного объекта | рядом «море/река/гора/…» — не населённый пункт |
| новость про США → Краснодар (0.35) | «место не названо → город источника» для портала миллионного города | запасная привязка только для источников сёл, районов и городов < 100 тыс. |
| «Псковская Область», «Ню Йорк», «Тел-Авив» | регистр и эвристика русских названий | миграции 008–010 и новая эвристика |
| «главой **МИДа**» → город Mead (США) | аббревиатура в падежной форме | «МИДа», «ЦБе» тоже аббревиатуры |
| «В **Раде** назвали…» → Радом (Польша) | лемма «Рада» совпала с «Радом» | органы власти (Рада, Дума, Сейм…) не места |
| «Инвестор **«Розы Хутор»**» → деревня Роза | курорт в кавычках | короткое название в «ёлочках» — бренд, клуб, проект |
| «Тренер **Артём** Сусленков» → город Артём | имя рядом с фамилией | имя + фамилия — человек при любом размере города |
| «высказалась о **Зеленском**» (в тексте «о Владимире Зеленском») → хутор | у формы есть случайный разбор-топоним | «один смысл на текст»: слово рядом с именем — человек во всей статье |

Последний прогон (все правки, кроме «один смысл на текст»): 15 рабочих источников, 1326 материалов, 70 %
привязаны. Доля ниже, чем в первом прогоне (81 %), за счёт снятых ложных привязок. Непривязанные материалы
кубанских СМИ в выборке действительно без места (интерьер, грибы, цифровой рубль). Из списка ошибок первого
прогона остался только случай с «Зеленском», закрытый последним правилом. Эталон — 45/45 после каждой правки.

Уже собранные на сервере старым кодом материалы пересчитываются миграцией 011 при следующем обновлении.

### Telegram, мир, GDELT (прогоны 6 и 7)

Прогон 6 (новые кандидаты): 51 источник прошёл проверку, из них 10 федеральных Telegram-каналов и 8 каналов
кубанских изданий, найденных на их сайтах. Найдено три ошибки:

| Что было | Причина | Исправление |
|---|---|---|
| канал Известий отклонён: «expected Известия, got IZ.RU ✔» | канал переименован | `expect_title` принимает список названий |
| РБК Краснодар → канал `rbc_news`, МЧС России → второй раз `mchs_official` | сайт регионального издания ссылается на федеральный канал; канал уже есть в списке | канал, уже известный по адресу, не добавляется второй раз |
| GDELT: тайм-аут | GDELT отвечает 20–40 с под нагрузкой | тайм-аут 45 с только для GDELT |

Прогон 7 (после правок): 51 источник, 2474 материала, 78 % привязаны к месту. Известия проходят, ложных каналов
`kuban-rbc-tg` и `mchs-rf-tg` нет. GDELT с раннера GitHub отвечает 429 (лимит на адрес, общий для раннеров);
по правилам GDELT запросы к нему теперь идут не чаще раза в 6 с. Сайты администраций края, Краснодара и
Сочи из-за рубежа недоступны, у сайтов МЧС последние записи многолетней давности, ТАСС отвечает 403 — они не
включены.

Если канал попал в список сервера по старому правилу, `--keep-previous` больше его не держит, а
`load-sources --prune` выключает (тест `test_previous_channel_stays_only_while_its_site_names_no_other`).

### Живая Земля и превью (Playwright, SwiftShader)

Скриншоты тёмной и светлой темы на ширине 1440 и 390 px: звёздное небо, ночная сторона по текущему положению
Солнца, огни городов на ночной стороне, горячие точки, пульс планеты и бегущая строка, превью в ленте и в
карточке события (проверено на локальных тестовых картинках). Картинка, которая не загрузилась, исчезает, не
оставляя пустой рамки. Приёмочный сценарий — 14/14 после всех изменений.

## Что не проверено

- Полный импорт GeoNames проверен на настоящем дампе RU (418 797 населённых пунктов, на сервере и в
  `live-check`); другие страны — только тестом на файлах формата GeoNames.
- Мобильные устройства: проверены ширины 360 и 390 px в Chromium, реальные телефоны — нет.
- Длительная работа (сутки и больше) и несколько процессов API под нагрузкой.
- Тёмный «застывший» прямоугольник в панели при плавной прокрутке: пойман 2 раза примерно за 10 записей в headless
  Chromium с программным рендерингом (SwiftShader). В DOM такого элемента нет; 6 контрольных прогонов (с
  `backdrop-filter` и без) чистые. Вероятно, это артефакт программного композитинга; на реальной GPU не проверялось.
